# Kiro IDE

Kiro IDE uses Markdown custom agents in `.kiro/agents/`.

Combine the surface-specific agent example here with the shared Steering, Skills and MCP
configuration from `common/example-workspace/`.
