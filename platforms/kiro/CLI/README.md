# Kiro CLI

Kiro CLI is independently installable and uses JSON custom-agent definitions.

Combine the CLI agent under `example-workspace/.kiro/agents/` with the shared Steering, Skills and
MCP configuration from `common/example-workspace/`.

CLI-only user settings live in `~/.kiro/settings/cli.json`.
