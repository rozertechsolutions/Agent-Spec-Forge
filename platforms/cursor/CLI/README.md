# Cursor CLI

Cursor CLI can be installed and used independently. It consumes the shared repository Rules and
MCP configuration from `common/example-workspace/`.

CLI session options remain CLI/runtime choices rather than duplicated project configuration.
