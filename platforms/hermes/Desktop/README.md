# Hermes Desktop

Hermes Desktop uses the shared Hermes agent configuration under `~/.hermes/`.

Apply the example from `common/user-home/.hermes/` to the user's home configuration only after the
user adapts provider/model choices and other local preferences.
