# Hermes CLI/TUI

Hermes CLI uses the same `~/.hermes/` configuration as Desktop.

The CLI can be used independently. Invocation flags can override configuration for a single run,
but those transient options are not duplicated as repository files here.
