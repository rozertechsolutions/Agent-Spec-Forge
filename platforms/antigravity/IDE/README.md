# Antigravity IDE

The IDE consumes the workspace configuration in `common/example-workspace/.agents/`.

This folder is intentionally limited to IDE-specific notes because Rules, Skills and workspace MCP
configuration are shared with the Antigravity agent environment rather than duplicated here.
