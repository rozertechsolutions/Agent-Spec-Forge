# Antigravity CLI

The CLI can run independently of the IDE and consumes the workspace customizations shown under
`common/example-workspace/.agents/`.

CLI-only persistent preferences live in:
`~/.gemini/antigravity-cli/settings.json`.

The included settings example keeps approvals enabled and enables terminal sandboxing.
