# Claude Code CLI

Use the configuration from `common/example-workspace/`.

The CLI is independently usable and shares the same project customizations with Claude Code IDE
integrations, so no duplicate copy is maintained here.
