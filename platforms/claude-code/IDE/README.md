# Claude Code IDE

Use the configuration from `common/example-workspace/`.

The IDE integration shares Claude Code project configuration with the CLI. This folder therefore
does not duplicate `CLAUDE.md`, agents, rules or skills.
