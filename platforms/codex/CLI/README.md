# Codex CLI

Use the shared repository configuration in `common/example-workspace/`.

The CLI can run independently of the IDE extension and discovers the same repository-level Codex
configuration. CLI invocation flags and interactive choices are intentionally not represented as
project files.
