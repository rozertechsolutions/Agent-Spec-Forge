# Codex IDE extension

Use the shared repository configuration in `common/example-workspace/`.

`AGENTS.md`, project agents, Skills, Rules and `.codex/config.toml` are repository-level
customizations and should not be duplicated only because Codex is being used from the IDE.
