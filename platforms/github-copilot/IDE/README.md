# GitHub Copilot IDE

Use the repository customizations in `common/example-workspace/`.

Supported IDEs can consume repository instructions and custom agents without maintaining a
separate copy solely for the IDE surface.
