# GitHub Copilot CLI

Copilot CLI can run independently and supports repository custom instructions, Skills and custom
agents. Use the shared configuration in `common/example-workspace/`.

CLI-only user configuration belongs under `~/.copilot/` when needed; this example does not create
global user files because the package is intended to remain portable and repository-scoped.
