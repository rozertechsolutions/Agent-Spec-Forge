# Capability Mapping

Map each neutral capability to an explicitly selected local runtime feature. Keep every optional capability disabled until the user authorizes it. If the runtime cannot enforce a permission, treat the capability as unavailable rather than relying on prompt text alone.
