schema_version: '1.0'
id: safety
rules:
- no secrets, credentials, real endpoints, account IDs, subscription IDs, or project IDs
- no execution of cloud CLIs, Docker, Kubernetes, Jenkins, Terraform, OpenTofu, Pulumi, Ansible, scanners, hooks, MCP servers, builds, tests, deployments, or generated configurations
- integrations remain disabled by default
