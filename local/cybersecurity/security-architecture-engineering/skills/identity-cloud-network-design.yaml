manifest_type: skill
schema: ../schemas/skill.schema.json
id: identity-cloud-network-design
classification: native
mission: Draft target-state identity, privileged access, cloud, platform, network, and communications security architecture.
scope: [identity, privileged_access, cloud_platform, network, communications]
inputs: [identity_flows, privilege_model, platform_topology, network_paths, connectivity_requirements]
outputs: [target_state_design, control_pattern_map, segmentation_review, privilege_notes, dependency_log]
owner: identity-cloud-network-agent
reviewers: [independent-architecture-reviewer]
tools: [tools/project-read.yaml, tools/static-discovery.yaml]
permissions: [read_only, approval_required_for_write]
stop_conditions: [live_configuration_requested, access_change_requested, monitoring_operation_requested]
prohibited_actions: [grant_access, change_configuration, operate_network_controls]

