# Gemini CLI Application, Product, and DevSecOps Security

Native Gemini CLI package for Cybersecurity Area 03 using `GEMINI.md`, `.gemini/agents`, and `.gemini/skills`. No MCP server, shell tool wrapper, scanner, pipeline, or external integration is configured.

