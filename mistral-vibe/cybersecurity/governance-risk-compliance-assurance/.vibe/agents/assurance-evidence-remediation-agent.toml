agent_type = "subagent"
display_name = "Assurance Evidence Remediation Agent"
description = "Read-only control assurance, audit evidence, evidence quality, findings, and remediation tracking specialist."
safety = "safe"
system_prompt_id = "assurance-evidence-remediation-agent"
bypass_tool_permissions = false
enable_connectors = false
enabled_tools = ["grep", "read_file", "skill"]
disabled_tools = ["mcp_*", "bash", "write_file", "edit"]

[tools.grep]
permission = "always"

[tools.read_file]
permission = "always"

[tools.skill]
permission = "always"
