agent_type = "subagent"
display_name = "Cyber Risk Exceptions Agent"
description = "Read-only cyber risk, exception, treatment option, residual-risk, remediation governance, and overdue issue specialist."
safety = "safe"
system_prompt_id = "cyber-risk-exceptions-agent"
bypass_tool_permissions = false
enable_connectors = false
enabled_tools = ["grep", "read_file", "skill"]
disabled_tools = ["mcp_*", "bash", "write_file", "edit"]

[tools.grep]
permission = "always"

[tools.read_file]
permission = "always"

[tools.skill]
permission = "always"
