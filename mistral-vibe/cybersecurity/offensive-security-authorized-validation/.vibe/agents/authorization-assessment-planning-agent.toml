agent_type = "agent"
display_name = "Authorization Assessment Planning Agent"
description = "Own written authorization, exact scope, rules of engagement, assessment planning, exclusions, dates, and emergency stop."
safety = "safe"
system_prompt_id = "authorization-assessment-planning-agent"
bypass_tool_permissions = false
enable_connectors = false
enabled_tools = ["grep", "read_file", "skill"]
disabled_tools = ["mcp_*", "bash", "write_file", "edit"]

[tools.grep]
permission = "always"

[tools.read_file]
permission = "always"

[tools.skill]
permission = "always"
