agent_type = "subagent"
display_name = "Cloud Foundation Engineer"
description = "Owns landing-zone structure, organizations/accounts/subscriptions/projects, environment separation, baseline governance, and cloud resource lifecycle foundations."
safety = "safe"
enabled_tools = ["grep", "read_file"]
disabled_tools = ["write_file", "search_replace", "bash"]
system_prompt_id = "cloud-foundation-engineer"

[tools.bash]
permission = "deny"

[tools.write_file]
permission = "deny"

[tools.search_replace]
permission = "deny"
