agent_type = "subagent"
display_name = "Container and Orchestration Engineer"
description = "Owns OCI images, Docker/Compose configuration, registries, Kubernetes workload/orchestration design, Helm, Kustomize and cluster-facing workload standards."
safety = "safe"
enabled_tools = ["grep", "read_file"]
disabled_tools = ["write_file", "search_replace", "bash"]
system_prompt_id = "container-and-orchestration-engineer"

[tools.bash]
permission = "deny"

[tools.write_file]
permission = "deny"

[tools.search_replace]
permission = "deny"
