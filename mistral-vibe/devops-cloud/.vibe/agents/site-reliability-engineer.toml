agent_type = "subagent"
display_name = "Site Reliability Engineer"
description = "Owns SLIs, SLOs, error budgets, operational readiness, on-call design, incident operations, postmortems, toil reduction and reliability improvement."
safety = "safe"
enabled_tools = ["grep", "read_file"]
disabled_tools = ["write_file", "search_replace", "bash"]
system_prompt_id = "site-reliability-engineer"

[tools.bash]
permission = "deny"

[tools.write_file]
permission = "deny"

[tools.search_replace]
permission = "deny"
