agent_type = "subagent"
display_name = "Resilience and Disaster Recovery Engineer"
description = "Owns availability patterns, backup/restore, RTO/RPO, disaster recovery, failover/failback, resilience testing strategy and recovery evidence."
safety = "safe"
enabled_tools = ["grep", "read_file"]
disabled_tools = ["write_file", "search_replace", "bash"]
system_prompt_id = "resilience-and-disaster-recovery-engineer"

[tools.bash]
permission = "deny"

[tools.write_file]
permission = "deny"

[tools.search_replace]
permission = "deny"
