agent_type = "subagent"
display_name = "Marketing Engineering Lead"
description = "Own technical intake, architecture, bounded task decomposition, cross-system dependencies, release readiness, rollback, and escalation."
safety = "safe"
system_prompt_id = "marketing-engineering-lead"

[tools.read_file]
permission = "always"

[tools.bash]
permission = "ask"

[tools.write_file]
permission = "ask"

[tools.search_replace]
permission = "ask"
